# Lubin Investment (investment-website)

Application d'analyse fondamentale d'actions : monorepo pnpm (`apps/web`, `apps/api`, `apps/backtest`, `packages/*`). Lis d'abord `README.md` (stack, lancement) et `docs/seo/PLAN.md` pour le volet SEO.

## Agent skills

### Issue tracker

Issues et specs vivent dans les GitHub Issues du repo (CLI `gh`). See `docs/agents/issue-tracker.md`.

### Triage labels

Vocabulaire par défaut de mattpocock/skills : `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context : `CONTEXT.md` à la racine et `docs/adr/`, créés à la demande par `/grill-with-docs`. See `docs/agents/domain.md`.
